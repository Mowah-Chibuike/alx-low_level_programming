#include "main.h"

/**
 * main - prints its name followed my a new line
 * @argc: number of arguments passed to the function
 * @argv: arguments passed to the function
 *
 * Return: 0 if suceesful
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
